# Signaling Service (WIP)

- Existing implementation lives in `signaling.js` at the repository root.
- This directory will host the rewired Node entry with dependency injection and tests.
- Current usage: `node signaling.js` (kept for compatibility until migration is complete).
